spring:
  application:
    name: fox-task-manager

  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: ${FLYWAY_ENABLED}

server:
  port: ${APP_PORT:8080}